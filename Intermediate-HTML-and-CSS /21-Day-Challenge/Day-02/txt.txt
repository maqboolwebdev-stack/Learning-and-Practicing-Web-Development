I learned when to use rem and em, along with their drawbacks. The main difference is that em units depend on the parent element, which can lead to compounding issues, while rem units depend on the root element, making them more predictable. rem is generally preferred for font sizing to maintain consistency across the layout, whereas em can be useful for spacing when you want values to scale relative to the parent.